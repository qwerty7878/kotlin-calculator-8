package calculator.model

class StringCalculator {
    fun calculate(input: String?): Int {
//        0 구분 여부
        if (input.isNullOrEmpty()) {
            return 0
        }
//        return 문자열 구분자 처리 로직
        return 0
    }
}